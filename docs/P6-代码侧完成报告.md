# P6 代码侧完成报告

日期：2026-08-17

范围：P6 修订八项的代码、自动化回归、发布与安全收尾。用户目标环境验收另列在文末，未以本报告代替。

## 结论

P6 代码侧收官条件已满足：Release 重建为 0 错误 / 0 警告；全 harness 为 292 通过 / 0 失败 / 1 跳过；self-contained 便携包已重新生成。原工作区的单实例、文件关联、随机插队与无缝预载半成品已合并进同一版本线。

## 收尾内容

- 单实例命名管道按用户 SID + 会话隔离；第二实例转交文件后退出；启动期间和运行期间的外部文件统一进入 FIFO 队列，已有曲目采用“下一首播放”入队语义。
- 文件关联继续使用 HKCU，覆盖九种音频格式、ProgID、OpenWithProgids、Capabilities、RegisteredApplications 与关联缓存刷新。
- 随机模式插队保持批次顺序并恢复原随机序列；插队会撤销旧预载，后台预载用 generation 校验，作废任务只能释放解码句柄。
- 任务栏播放/暂停状态与进度状态同步；三层未处理异常均记录脱敏文本并提供日志路径提示，后台任务弹窗有节流。
- 原 21 条 Release 分析警告全部修复：1 条 `CS8604` 与 20 条重复的 `MVVMTK0034`，0 条豁免。
- 新增 `.githooks/pre-commit` 与 `tools/check-staged-key-prefix.ps1`。检查范围是完整暂存区；仅保留 PLAN 规则文本与 Harness 脱敏夹具中的既有前缀说明，其他位置命中即拒绝。当前仓库已启用 `core.hooksPath=.githooks`。
- ChKSz 客户端的 URL、裸 Key、网络异常和全局异常日志均经过脱敏；测试配置和日志没有进入 Git。

## 验证记录

| 项目 | 结果 |
| --- | --- |
| `dotnet clean Player.sln -c Release` + Release Rebuild | 0 错误 / 0 警告 |
| seamless | 34 通过 |
| lyrics | 113 通过 |
| grouping | 13 通过 |
| theme | 22 通过 |
| shortcuts | 33 通过 |
| downloads | 29 通过 |
| netfail | 4 通过 |
| dsp | 44 通过 / 1 跳过（目标 ASIO 硬件听测） |
| 合计 | **292 通过 / 0 失败 / 1 跳过** |

### ChKSz 链路

真实搜索链路探针：**成功**（HTTP 200）。响应额度头：`X-Quota-Free-Remaining=266`、`X-Quota-Paid-Remaining=0`。报告、日志和命令输出均不记录 Key 本体。

### 钩子自测

合法的 PLAN/Harness 既有文本通过（退出码 0）；故意暂存一个含保留前缀的临时文件被拦截（退出码 1，仅输出路径和行号）；测试文件已移除，恢复后再次通过。

### 安全核对

- 当前 Git 跟踪内容没有配置 Key；工作区日志扫描命中 0 个 Key。
- 频谱链路仍遵守 DSP 红线：频谱只从 mixer DSP tap 复制样本到 SPSC ring，FFT 在后台分析器执行；解码流/混音流没有直接读取频谱数据。
- 最终 zip 不含 `data` 或 `format-test`，并包含 self-contained .NET / Windows Desktop 运行时。

## 当前机器验收进度（2026-08-17）

- 文件关联三路径首轮通过：Shell 双击 FLAC 在 `147 ms` 内启动正式 `publish\Player.exe`；运行中用两首 FLAC 多选转交时第二进程 `338 ms` 内退出，始终只保留 `1` 个 Player；直接把单曲参数传给 `Player.exe` 的拖放等价路径也通过。日志记录导入 `2` 首和 `1` 首并入队，未出现 `ERR`、`FTL`、Unhandled、Exception 或 `WRN`。
- 最终 self-contained zip 解压到无 `data` 的临时目录后启动，首次出现 Player 窗口耗时 `1295 ms`；自动生成配置的 API Key 长度为 `0`。这证明当前机器的冷启动已低于 `1.5 s`，不替代新 Windows 用户的完整目验。
- 用 `10,010` 个同卷测试音频条目（10 个独立源文件各 1,000 个硬链接）运行 library harness：全量扫描 `0.6 s`，增量扫描 `0.0 s`，10 项库/歌单断言全部通过。该夹具已清理，不代表真实万曲标签多样性。
- 播放 FLAC 做 `60 s` 内存基线：工作集 `209.0 → 205.4 MB`（`-3.6 MB`），Private Bytes `108.1 → 103.5 MB`（`-4.6 MB`）；进程已结束，无残留。该短测不替代 2 小时频谱连续播放。

## 最终交付物

- 文件：`Player-v1.0.0-win-x64.zip`
- 大小：77,837,863 字节，273 项
- SHA-256：`55837748159D4B21AAAFAEA5B890537B559890AAD2AD8897D80079B4C5AED365`
- 包内 README 来自 `docs/用户指南.md`，已含快速上手、ASIO 摘要与许可声明。

## 仍待用户实机验收

1. 任务栏缩略图按钮与进度条目验（需要用户目视点击上一首/播放暂停/下一首并观察进度状态）。
2. 频谱迷你窗连续播放两小时的内存稳定性。
3. 新 Windows 用户或干净电脑的完整首次运行体验（引导、输出设备、文件关联及无残留退出）。

上述剩余三项完成后，再做规划方全项目终审与 v1.0 定版。
