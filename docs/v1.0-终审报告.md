# Lyra v1.0 规划方终审报告

日期：2026-08-18 · 终审对象：提交 `b7301fa`（feat: finalize Lyra rename and release wiring）
交付物：`Lyra-v1.0.0-win-x64.zip`（285 项，77,767,068 字节，SHA-256 `A8D3…6BAB`）
审计方：规划方（Fable）· 全程独立核验，不采信未经验证的执行方声明

## 一、终审范围与结论

| 审计域 | 结论 | 关键证据 |
| --- | --- | --- |
| 合规（Key/敏感数据） | ✅ | Key 保留前缀在工作区仅规则条文与脱敏夹具 2 处命中；git 全历史无真实 Key（含改写后复扫）；pre-commit 钩子 fail-closed（规划方 Linux 亲测退出码 1，且本报告初稿因含前缀字面量被其正确拦截——已改写合规）；发布脚本敏感熔断实存 |
| 许可 | ✅ | GPLv2 的 `bass_aac.dll` 已剔除（AAC 走系统解码器）；THIRD-PARTY-NOTICES + licenses/ 齐备；README/用户指南含 BASS 非商业、GD CC BY-NC、ChKSz 与开源组件声明 |
| 交付物完整性 | ✅ | zip SHA 与报告逐字一致；含 `Lyra.exe` 无 `Player.exe`；恰好 9 个白名单 BASS DLL；无 data/配置/日志/数据库/Key 前缀 |
| 改名正确性 | ✅ | AssemblyName/版本/产品元数据=Lyra 1.0.0；ICO 六帧（256→16，32bpp PNG）结构解析验证；升级期双互斥体桥+旧管道监听；不触碰 Windows UserChoice；data/ 零迁移 |
| 退出语义 | ✅ | 用户实机：主窗/托盘/桌面歌词/迷你窗任意组合退出无残留；文件关联三路径实测（含 92ms 退出接替） |
| 性能 | ✅ | 用户实机六项通过：启动≤1.5s、2h 内存稳定（<300MB）、万曲级扫描、干净机 zip 引导 |
| 文档 | ✅ | README/用户指南刷新至 v1.0 终态（新功能全覆盖、废弃描述清零、data/ 迁移说明、hooksPath 说明） |
| 自动化基线 | ⚠️ 有条件通过 | 执行方 `-warnaserror` 构建 0 警告 + 九模式 harness 348/0/1 两次复现；**规划方独立复算因沙盒网络持续受限未完成，挂账**（见三-1） |

**终审结论：Lyra v1.0 定版通过（有条件项一项，见下）。**

## 二、遗留开放项（不阻塞定版）

1. **ChKSz Key 401**：执行方实测当前配置的 Key 未被上游接受（错误处理与脱敏均正确）。用户侧动作：到 api.chksz.com 面板确认/重置 Key → 填入 Lyra 设置-在线 → 点"测试连接"见额度即闭环。GD 为主力源，此项不影响日常功能。
2. **规划方复算挂账**：沙盒网络恢复后补跑独立编译与九模式 harness 复算并回写本报告；若与 348/0/1 存在差异，按缺陷流程追溯。
3. Backlog 备忘：GD 多端点故障转移、LX Music 协议适配器、Embeat 推荐系统、快捷键改绑之外的自定义项。

## 三、里程碑存档

P0 骨架（2026-08-13）→ P1 媒体库 → P2 ASIO/无缝 → P3 在线接入+歌词 → UI-R 界面重构（R0–R5）→ L1 歌词三连 → L2 系统集成 → P4 在线三件套 → L3 个性化+迷你窗 → P6 发布工程 → **Lyra v1.0（2026-08-18）**。
执行方沿革：Opus（P0–P2）→ DeepSeek（P3–L3）→ GPT-5.6-sol（P6–v1.0）；规划与审计：Fable。六天，三个执行模型，一位甲方，一个规划方——纪律比模型更重要。
